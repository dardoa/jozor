# Development Scripts

This directory contains various utility scripts used for development, testing, and deployment.

## Contents
- **dev/**: Contains development middleware, such as the local API proxy `localApiProxyMiddleware.ts`.
- Additional scripts for CI/CD or local automation can be added here.

## Usage
These scripts are generally invoked by npm commands in the root `package.json` or by configurations (like Vite or Playwright).
