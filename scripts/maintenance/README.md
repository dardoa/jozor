# Maintenance Scripts

This folder contains one-off or rarely used maintenance utilities that are not part of the normal dev, build, or test workflow.

Keep root-level scripts limited to commands that are wired into `package.json` or are required by tooling.
